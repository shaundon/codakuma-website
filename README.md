# Codakuma Website

## Prerequisites

- Ruby (see `.ruby-version` for the required version). We recommend [rbenv](https://github.com/rbenv/rbenv) to manage Ruby versions:
  ```
  brew install rbenv
  rbenv install
  ```

## How to run

```
bundle install
bundle exec jekyll serve
```

The site will be available at `http://localhost:4000`.
